#pragma once

namespace Hail
{
	// TODO make some kind of macro to define these textures, as they are defined by the engine and are fixed
	// Set 0 
	enum class eGlobalTextures
	{
		count
	};
	// Set 1
	enum class eMaterialTextures
	{
		FullscreenPassTarget,
		count
	};
}